version: '3.7'
services:
  # Base de Dados Relacional
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: clinica_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

  # Servidor HAPI FHIR
  hapi-fhir:
    image: hapiproject/hapi:latest
    ports:
      - "9090:8080"

  # ----------------------------------------------------
  #  EHRBASE
  # ----------------------------------------------------

  # Base de Dados para o EHRbase
  ehrbase-db:
    image: postgres:16
    environment:
      POSTGRES_DB: ehrbase
      POSTGRES_USER: ehrbase
      POSTGRES_PASSWORD: ehrbase
    ports:
      - "5433:5432"
    volumes:
      - ./init_ehrbase.sql:/docker-entrypoint-initdb.d/init_ehrbase.sql

  # Servidor EHRbase
  ehrbase:
    image: ehrbase/ehrbase:2.31.0
    depends_on:
      - ehrbase-db
    ports:
      - "8082:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://ehrbase-db:5432/ehrbase
      SPRING_DATASOURCE_USERNAME: ehrbase
      SPRING_DATASOURCE_PASSWORD: ehrbase
      SPRING_FLYWAY_USER: ehrbase
      SPRING_FLYWAY_PASSWORD: ehrbase
      SECURITY_AUTHTYPE: none

  # Serviço FastAPI de Integração
  api:
    build: .
    container_name: integration-service
    ports:
      - "5000:5000"
    depends_on:
      - hapi-fhir
      - ehrbase
      - db
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_NAME=clinica_db
      - DB_USER=user
      - DB_PASS=password
      - EHRBASE_URL=http://ehrbase:8081/ehrbase/rest/openehr/v1
      - FHIR_SERVER_URL=http://hapi-fhir:8080/fhir

  # ----------------------------------------------------
  #  DASHBOARD STREAMLIT
  # ----------------------------------------------------
  streamlit:
    build:
      context: .
      dockerfile: Dockerfile.streamlit
    container_name: pce-dashboard
    ports:
      - "8501:8501"
    depends_on:
      - api
      - hapi-fhir
      - ehrbase
    environment:
      # O Streamlit (dentro do Docker) comunica com os outros serviços
      # pelos nomes internos da rede Docker
      - FASTAPI_URL=http://api:5000
      - HAPI_FHIR_URL=http://hapi-fhir:8080/fhir
      # Para o EHRbase, o Streamlit usa a URL interna do Docker
      - EHRBASE_URL_LOCAL=http://ehrbase:8080/ehrbase/rest/openehr/v1
    restart: unless-stopped